<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Volunteer Opportunity Form</title>
    <link rel="icon" href="/images/logo.png" type="image/x-icon">
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <!-- HEADER -->
    <header style="background-color: #3b7a57; padding: 15px 20px; color: white; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);">
        <div style="display: flex; align-items: center; justify-content: space-between;">
            <!-- Logo and Website Name -->
            <a href="/">
                <div style="display: flex; align-items: center;">
                    <img src="/images/logo.png" alt="Website Logo" class="logo">
                    <h1 style="margin: 0; font-size: 24px; color: white;">TURTLE SHELTER PROJECT</h1>
                </div>
            </a>

            <!-- Navigation Links -->
            <nav>
                <ul style="list-style: none; display: flex; margin: 0; padding: 0;">
                    <li style="margin: 0 10px;"><a href="/" style="color: white; text-decoration: none; font-weight: bold;">Home</a></li>
                    <li style="margin: 0 10px;"><a href="/jens-story" style="color: white; text-decoration: none; font-weight: bold;">Jen's Story</a></li>
                    <li style="margin: 0 10px;"><a href="/sponsors" style="color: white; text-decoration: none; font-weight: bold;">Sponsors</a></li>
                    <li style="margin: 0 10px;"><a href="/about-us" style="color: white; text-decoration: none; font-weight: bold;">About Us</a></li>
                    <li style="margin: 0 10px;"><a href="/how-to-help" style="color: white; text-decoration: none; font-weight: bold;">How You Can Help</a></li>
                    <li style="margin: 0 10px;"><a href="/login" style="color: white; text-decoration: none; font-weight: bold;">For Team Members</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Volunteer form content -->
    <div class="container mt-5">
        <h1 class="mb-4">Volunteer Opportunity Form</h1>
        <form action="/volunteer" method="POST" onsubmit="return validateForm()">
            <div class="mb-3">
                <label for="first_name" class="form-label">First Name:</label>
                <input type="text" class="form-control" id="first_name" name="first_name" required>
            </div>

            <div class="mb-3">
                <label for="last_name" class="form-label">Last Name:</label>
                <input type="text" class="form-control" id="last_name" name="last_name" required>
            </div>

            <div class="mb-3">
                <label for="phone" class="form-label">Phone Number:</label>
                <input type="tel" class="form-control" id="phone" name="phone" required>
            </div>

            <div class="mb-3">
                <label for="email" class="form-label">Email Address:</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>

            <div class="mb-3">
                <label for="street_address" class="form-label">Street Address:</label>
                <input type="text" class="form-control" id="street_address" name="street_address" required>
            </div>

            <div class="mb-3">
                <label for="city" class="form-label">City:</label>
                <input type="text" class="form-control" id="city" name="city" required>
            </div>

            <div class="mb-3">
                <label for="state" class="form-label">State:</label>
                <input type="text" class="form-control" id="state" name="state" required>
            </div>

            <div class="mb-3">
                <label for="zip" class="form-label">Zip Code:</label>
                <input type="text" class="form-control" id="zip" name="zip" required>
            </div>

            <div class="mb-3">
                <label class="form-label">How did you hear about this opportunity?</label><br>
                <div class="form-check form-check-inline">
                    <!-- Radio Button for how they heard about it -->
                    <input type="radio" class="form-check-input" id="referral_source_social_media" name="referral_source" value="Social Media" onclick="toggleOtherInput()" required>
                    <label for="referral_source_social_media" class="form-check-label">Social Media</label>
                </div>
                <div class="form-check form-check-inline">
                    <input type="radio" class="form-check-input" id="referral_source_friend" name="referral_source" value="Friend" onclick="toggleOtherInput()">
                    <label for="referral_source_friend" class="form-check-label">Friend</label>
                </div>
                <div class="form-check form-check-inline">
                    <input type="radio" class="form-check-input" id="referral_source_event" name="referral_source" value="Event" onclick="toggleOtherInput()">
                    <label for="referral_source_event" class="form-check-label">Event</label>
                </div>
                <div class="form-check form-check-inline">
                    <input type="radio" class="form-check-input" id="referral_source_other" name="referral_source" value="" onclick="toggleOtherInput()">
                    <label for="referral_source_other" class="form-check-label">Other</label>
                </div>
                <input type="text" class="form-control mt-2" id="referral_source_other_input" name="referral_source_other-text" placeholder="Please specify" disabled>
            </div>

            <div class="mb-3">
                <label for="sewing_level" class="form-label">What is your sewing experience?</label>
                <select id="sewing_level" name="sewing_level" class="form-select" required>
                    <option value="N">No sewing</option>
                    <option value="B">Beginner</option>
                    <option value="I">Intermediate</option>
                    <option value="E">Experienced</option>
                </select>
            </div>

            <div class="mb-3">
                <label for="volunteer_hours" class="form-label">How many hours would you be willing to volunteer per month?</label>
                <input type="number" class="form-control" id="volunteer_hours" name="volunteer_hours" min="1" required>
            </div>

            <div class="mb-3">
                <!-- Checkbox for newsletter prompt -->
                <label for="newsletter" class="form-check-label">Would you like to subscribe to our monthly newsletter?</label><br>
                <input type="checkbox" class="form-check-input" id="newsletter" name="newsletter">
            </div>

            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>

    <!-- FOOTER -->
    <footer style="background-color: #3b7a57; padding: 15px 20px; color: white; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);">
        <p>Turtle Shelter Project - PO Box 382, Kaysville, UT 84037</p>
        <p>Phone: (801) 872-3190</p>
        <p>Email: <a href="mailto:turtleshelterproject@gmail.com">turtleshelterproject@gmail.com</a></p>
        <p>
            <a href="https://www.facebook.com/turtleshelterproject/" target="_blank">Visit our Facebook Page</a>
        </p>
    </footer>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script>
        // script to ensure that they are able to input what their "other" is
        function toggleOtherInput() {
            const otherOption = document.getElementById('referral_source_other');
            const otherInput = document.getElementById('referral_source_other_input');

            if (otherOption.checked) {
                otherInput.disabled = false;
                otherInput.required = true;

                // Update the value of the radio button to the text input dynamically
                otherInput.addEventListener('input', () => {
                    otherOption.value = otherInput.value.trim();
                });
            } else {
                otherInput.disabled = true;
                otherInput.required = false;
                otherInput.value = ''; // Clear the input if not selected
                otherOption.value = ''; // Reset the value of "Other" radio button
            }
        }

        // Function to validate the form inputs to match what is stored in the database
        function validateForm() {
            const phone = document.getElementById('phone').value.trim();
            const state = document.getElementById('state').value.trim();
            const hours = document.getElementById('volunteer_hours').value.trim();
        
            // Validation messages
            let messages = [];
        
            // Check phone number (numeric values only)
            if (!/^\d+$/.test(phone)) {
                messages.push('Phone number must contain only numeric values.');
            }
        
            // Check state (2 letters)
            if (!/^[A-Za-z]{2}$/.test(state)) {
                messages.push('State must be exactly two letters.');
            }
        
            // Check hours (greater than 0)
            if (isNaN(hours) || hours <= 0) {
                messages.push('Monthly hours must be greater than 0.');
            }
        
            // If there are validation errors, prevent form submission and show alerts
            if (messages.length > 0) {
                alert(messages.join('\n'));
                return false; // Prevent form submission
            }
        
            return true; // Allow form submission
        }
    </script>
</body>
</html>

